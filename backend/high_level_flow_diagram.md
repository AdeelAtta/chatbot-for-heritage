# Mohenjo-daro AI Chatbot & Image Generation System - High Level Flow Diagram

## Two-Phase System Overview

### Phase 1: Local Setup (One-time)
```
Raw Text Data → ingest.py → ChromaDB Database → Upload to GitHub
```

### Phase 2: Runtime on Streamlit Cloud
```
User Query → Streamlit App → ChromaDB (bundled) → LLM API → Response
```

---

## Phase 1: Local Setup & Knowledge Base Creation

```mermaid
flowchart TD
    subgraph RawData["1. RAW DATA SOURCES"]
        A[Mohenjo-daro<br/>Historical Texts] 
        B[Archaeological<br/>Records]
        C[Wikipedia,<br/>Academic Sources]
    end

    subgraph Processing["2. INGEST.PY SCRIPT"]
        A --> D[Text Loading<br/>LangChain]
        B --> D
        C --> D
        D --> E[Text Chunking<br/>RecursiveCharacter]
        E --> F[Embedding Generation<br/>sentence-transformers]
        F --> G[Vector Storage<br/>ChromaDB]
    end

    subgraph Output["3. OUTPUT"]
        G --> H[(chroma_db/<br/>Vector Database)]
        H --> I[Push to GitHub<br/>Repository]
    end

    subgraph Deploy["4. DEPLOYMENT"]
        I --> J[Streamlit Cloud<br/>Reads chroma_db/]
    end

    style RawData fill:#e3f2fd
    style Processing fill:#fff3e0
    style Output fill:#e8f5e9
    style Deploy fill:#fce4ec
```

---

## Phase 2: Runtime (User Interaction on Streamlit Cloud)

```mermaid
flowchart TD
    subgraph UI["1. USER INTERFACE"]
        A[User Question] --> B[Streamlit UI]
        B --> C[Chat Display<br/>Text Response]
        B --> D[Image Display<br/>Generated Image]
    end

    subgraph Runtime["2. RUNTIME PROCESSING"]
        A --> E[Intent Detection<br/>Image Needed?]
        E -->|Yes| F[Generate Image Prompt]
        E -->|No| G[Skip Image Gen]
        F --> H[Image Gen API<br/>FLUX.1-dev / SDXL]
        G --> C
        H --> D
    end

    subgraph AI["3. AI PIPELINE"]
        A --> I[RAG Retrieval<br/>Query ChromaDB]
        I --> J[Context Assembly<br/>Top-K Chunks]
        J --> K[LLM Processing<br/>Mistral / Llama]
        K --> C
        K --> F
    end

    subgraph External["4. EXTERNAL APIs"]
        K --> L[LLM API<br/>Hugging Face]
        H --> M[Image Gen API<br/>Hugging Face]
    end

    subgraph Storage["5. KNOWLEDGE BASE"]
        N[(ChromaDB<br/>Bundled in repo)]
        N -.->|Loads at| I
    end

    style UI fill:#e3f2fd
    style Runtime fill:#fff3e0
    style AI fill:#e8f5e9
    style External fill:#fce4ec
    style Storage fill:#e1f5fe
```

---

## Complete System Architecture

```mermaid
flowchart LR
    subgraph Setup["PHASE 1: LOCAL SETUP"]
        direction TB
        S1[Raw Text Files] --> S2[ingest.py]
        S2 --> S3[Chunking & Embedding]
        S3 --> S4[(chroma_db/)]
        S4 --> S5[GitHub Repo]
    end

    subgraph Cloud["PHASE 2: RUNTIME (Streamlit Cloud)"]
        direction TB
        R1[User Question] --> R2[Streamlit App]
        R2 --> R3[RAG Pipeline]
        R3 --> R4[LLM API]
        R4 --> R5[Text Response]
        R3 --> R6[Image Prompt]
        R6 --> R7[Image Gen API]
        R7 --> R8[Generated Image]
        S5 -.->|Deploys with| R2
    end
```

---

## System Components Summary

| Component | Phase | Purpose |
|-----------|-------|---------|
| **ingest.py** | Setup | Convert raw text → ChromaDB |
| ChromaDB | Setup+Runtime | Knowledge base storage |
| Streamlit | Runtime | Chat UI |
| LangChain | Setup+Runtime | RAG pipeline, chunking |
| LLM API | Runtime | Response generation |
| Image Gen API | Runtime | Visual content creation |
| Hugging Face | Runtime | Model hosting |

---

## File Structure

```
mohenjo-daro-chatbot/
├── data/
│   └── raw_mohenjo_daro_info.txt    ← Historical texts (Phase 1)
├── chroma_db/                       ← Generated by ingest.py (Phase 1)
│   └── ...
├── ingest.py                        ← Setup script (Phase 1)
├── app.py                           ← Main Streamlit app (Runtime)
├── requirements.txt
└── README.md
```

---

## Detailed Workflow

### Phase 1: Setup (Run Locally Once)
```
1. Collect raw text data about Mohenjo-daro
       ↓
2. python ingest.py
       ↓
3. Script loads text → chunks → embeds → stores in ChromaDB
       ↓
4. Commit chroma_db/ folder to GitHub
```

### Phase 2: Deployment & Runtime
```
1. Push to GitHub
       ↓
2. Connect to Streamlit Cloud
       ↓
3. Deploy app.py
       ↓
4. ChromaDB auto-loads from repo at startup
       ↓
5. Users interact in real-time
```

---

## Architecture Diagram (Text)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    MOHENJO-DARO AI CHATBOT SYSTEM                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ╔═══════════════════════════════════════════════════════════════════╗ │
│  ║  PHASE 1: LOCAL SETUP (One-time)                                  ║ │
│  ╠═══════════════════════════════════════════════════════════════════╣ │
│  ║                                                                   ║ │
│  ║   ┌──────────────┐     ┌──────────────┐     ┌──────────────┐      ║ │
│  ║   │   Raw Text   │────▶│  ingest.py  │────▶│  ChromaDB    │      ║ │
│  ║   │   Files      │     │             │     │  Database    │      ║ │
│  ║   └──────────────┘     └──────────────┘     └──────────────┘      ║ │
│  ║                                                     │              ║ │
│  ║                                                     ▼              ║ │
│  ║                                            ┌──────────────┐       ║ │
│  ║                                            │  GitHub Repo │       ║ │
│  ║                                            └──────────────┘       ║ │
│  ║                                                                   ║ │
│  ╚═══════════════════════════════════════════════════════════════════╝ │
│                                                                         │
│  ╔═══════════════════════════════════════════════════════════════════╗ │
│  ║  PHASE 2: RUNTIME (Streamlit Cloud)                                ║ │
│  ╠═══════════════════════════════════════════════════════════════════╣ │
│  ║                                                                   ║ │
│  ║   ┌──────────────────────────────────────────────────────────┐  ║ │
│  ║   │                  STREAMLIT INTERFACE                       │  ║ │
│  ║   │                                                              │  ║ │
│  ║   │   ┌────────────────────┐    ┌────────────────────────────┐ │  ║ │
│  ║   │   │  User Question     │    │  Chat History              │ │  ║ │
│  ║   │   │  (st.chat_input)   │    │  (st.chat_message)         │ │  ║ │
│  ║   │   └────────────────────┘    │  - Text Responses          │ │  ║ │
│  ║   │                               │  - Generated Images       │ │  ║ │
│  ║   │                               └────────────────────────────┘ │  ║ │
│  ║   └──────────────────────────────────────────────────────────┘  ║ │
│  ║                                │                                  ║ │
│  ║                                ▼                                  ║ │
│  ║   ┌──────────────────────────────────────────────────────────┐  ║ │
│  ║   │              LANGCHAIN RAG PIPELINE                        │  ║ │
│  ║   │                                                              │  ║ │
│  ║   │   ┌───────────────┐        ┌─────────────────┐             │  ║ │
│  ║   │   │  Semantic     │───────▶│  Context        │             │  ║ │
│  ║   │   │  Retrieval    │        │  Assembly       │             │  ║ │
│  ║   │   │  (ChromaDB)   │        │                 │             │  ║ │
│  ║   │   └───────────────┘        └─────────────────┘             │  ║ │
│  ║   │          │                        │                       │  ║ │
│  ║   │          │                        ▼                       │  ║ │
│  ║   │          │              ┌─────────────────┐               │  ║ │
│  ║   │          │              │   LLM           │               │  ║ │
│  ║   │          │              │ (Mistral/Llama) │               │  ║ │
│  ║   │          │              └─────────────────┘               │  ║ │
│  ║   │          │                     │                         │  ║ │
│  ║   │          │           ┌──────────┴──────────┐              │  ║ │
│  ║   │          │           ▼                     ▼              │  ║ │
│  ║   │          │   ┌──────────────┐    ┌───────────────┐       │  ║ │
│  ║   │          │   │    Text      │    │  Image Prompt │       │  ║ │
│  ║   │          │   │   Response   │    │   Generator   │       │  ║ │
│  ║   │          │   └──────────────┘    └───────┬───────┘       │  ║ │
│  ║   └──────────┼───────────────────────────────┼───────────────┘  ║ │
│  ║              │                               │                 ║ │
│  ║              ▼                               ▼                 ║ │
│  ║   ┌─────────────────────────────────────────────────────────┐  ║ │
│  ║   │              HUGGING FACE APIS                           │  ║ │
│  ║   │                                                           │  ║ │
│  ║   │   ┌───────────────────────┐  ┌────────────────────────┐ │  ║ │
│  ║   │   │    LLM API            │  │   Image Generation API  │ │  ║ │
│  ║   │   │  Mistral-7B/Llama-3   │  │   FLUX.1-dev / SDXL     │ │  ║ │
│  ║   │   └───────────────────────┘  └────────────────────────┘ │  ║ │
│  ║   └─────────────────────────────────────────────────────────┘  ║ │
│  ║                                                                   ║ │
│  ║   ┌──────────────────────────────────────────────────────────┐  ║ │
│  ║   │  ChromaDB (loaded from GitHub at app startup)             │  ║ │
│  ║   └──────────────────────────────────────────────────────────┘  ║ │
│  ║                                                                   ║ │
│  ╚═══════════════════════════════════════════════════════════════════╝ │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Runtime Sequence (User Request)

```mermaid
sequenceDiagram
    participant User
    participant UI as Streamlit UI
    participant RAG as RAG Pipeline
    participant DB as ChromaDB (bundled)
    participant LLM as LLM API
    participant IMG as Image Gen API

    Note over UI,DB: App starts → ChromaDB auto-loads

    User->>UI: Enter Question
    UI->>RAG: Send Query
    RAG->>DB: Real-time Semantic Search
    DB-->>RAG: Top-K Relevant Context
    RAG->>LLM: Generate Response
    LLM-->>UI: Text Response
    UI->>User: Display Response

    alt Image Requested
        UI->>RAG: Check Intent
        RAG->>LLM: Generate Image Prompt
        LLM-->>RAG: Image Description
        RAG->>IMG: Send Prompt
        IMG-->>UI: Generated Image
        UI->>User: Display Image
    end
```
